// 当 feature 用 Provider 定義。domain には Provider を置かない。
// コピー後: feature → <feature_name> に置換し、必要な Provider を追加する。
// Provider を追加するときは import 'package:flutter_riverpod/flutter_riverpod.dart'; を追加する。

// 例: final featureSomethingProvider = Provider<DoSomethingUseCase>((ref) => ...);
