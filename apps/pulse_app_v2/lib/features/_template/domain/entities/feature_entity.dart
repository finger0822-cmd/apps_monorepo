// ドメインエンティティ。Flutter / Isar / Riverpod を import しない（純 Dart のみ）。
// コピー後: FeatureEntity を実エンティティ名に置換する。

/// 当 feature のドメインエンティティ。
class FeatureEntity {
  const FeatureEntity({required this.id});

  final String id;
}
