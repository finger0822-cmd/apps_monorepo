// ローカルデータ取得の窓口（Isar / SharedPreferences 等）。必要ならここに実装。
// コピー後: FeatureLocalDatasource を実名に置換する。

/// 当 feature のローカルデータソース。Repository 実装から利用する。
class FeatureLocalDatasource {
  // Future<Model?> get(...) / Future<void> put(...) 等
}
